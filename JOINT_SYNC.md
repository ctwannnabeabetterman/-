# 三项闭环修复与复现

本次在 `b5f8d71` 基础上实现 RX 到达差校正、有限窗口独立采样及持续联合同步。保留纯软件环境，不连接 SDR，不修改现有 Python 环境。

## 1. RX 到达对齐

默认 AP0→RX 为 50 ns，AP1→RX 为 75 ns。先发送两个已知时隙的探测 burst；仿真用相同相对时隙原点生成两个 RX 接收块。每个块由已知短码和双音组成，用短码确定整周期，用双音 QLS/LUT 细化接收起点。

RX 去除已知发送时隙差后反馈 `d_hat=t1_hat-t0_hat`。AP1 发射调度/数字延时命令为 `c_tx=-d_hat`。实际数据到达差为：

```text
delta_arrival = tau1 - tau0 - residual_clock_offset + c_tx
```

`c_tx` 包含差分路径和当前残余钟差，但不用于修改 AP1 的时钟。相位导频在此命令生效后生成。相位权重仍来自有噪声的 LS 估计和反馈量化。持续实验预留双探测及反馈的时间；这些探测按当前准静态钟差生成，不模拟该短探测期间的移动信道。

原来只报告复增益相位会把 25 ns 双音相消误报为 0°。现在保存三个量：实际 IQ 内积相位 `residual_phase_deg`、载频复增益相位 `carrier_phase_deg` 和归一化相关幅度 `waveform_coherence`。相位不替代时延和功率判据。

## 2. 独立时钟与有限接收窗口

`acquisition.py` 使用 SciPy CZT 在 ADC 均匀网格上求补零 DAC IQ 的带限 Fourier 插值：

```text
source_start = (rx_start_true - tx_start_true - tau) * fs * tx_rate
source_step  = tx_rate / rx_rate
```

`tx_rate` 和 `rx_rate` 是物理仿真参数，真实改变采样 IQ；估计器只接收 `Capture(samples, start_local_s, sample_rate_hz)`。窗口由发射公告时间戳、公开粗传播时延及 `receive_pretrigger_s` 决定。默认前置余量 2 µs，窗口为 burst 长度加两侧余量，可用 `receive_window_s` 改为固定长度。

固定短码为 128 个 BPSK 样点经五点 Hann 卷积，后接 32 样点保护间隔，再接双音。短码捕获归一化阈值默认 0.45；缺码、窗口过短、同步脉冲截断和无效精测峰都会报 `AcquisitionError`，不得当成零误差成功。窗口外的钟差不保证捕获；该版本未实现自动扩大窗口的协议。

`LinkDelayMeasurement.corrected_delay_s` 现在表示双音起点相对 ADC 首样点的本地时间，接收时间戳是它加 `capture_start_local_s`。不能把这个值直接当成真实单程传播时延。Monte Carlo 按新的时间戳原点评价误差。

`TwoWayConfig.processing_delay_s` 现在从完整同步脉冲接收完毕算至回复短码开始；四时间戳仍均标记双音起点。校正控制在回复脉冲完整接收后生效。无时钟漂移时处理时间仍在双向公式中抵消；有漂移时只在准静态近似下成立。

同步噪声以单位活动区功率定义；不因静默窗口或前缀长度而改变双音 SNR。粗码额外占用能量和带宽，因此不声称整个 burst 与论文单脉冲频谱完全相同，所画 CRLB 只对应双音的局部时延估计。

## 3. 持续联合同步

主程序仍保存四状态演示，另用 `run_joint_tracking()` 独立运行完整多轮流程：

1. 按 50 ms 周期推进具有速率随机游走的时钟和连续相位本振。
2. 从有限窗口 IQ 生成四时间戳，用接收时间戳及自身已发控制命令重建钟差。
3. 最近 8 个有效观测滑窗拟合速率，更新时钟速率和时间校正，控制生效时保持时钟连续。
4. 接收含非零残余 CFO 的频率参考，估计后更新连续相位本振控制。
5. RX 探测到达差并反馈发射延时，再接收相位导频，数据从导频结束加反馈延迟开始。
6. 每个周期内采样 9 个保持时刻，联合检查节点钟差、目标到达差、实际 IQ 相位与相干增益。

`JointTrackingConfig` 可配置轮数、种子、随机游走标准差、掉帧轮次、频偏突变轮次及大小。掉帧时保留旧控制命令，并照常评分；恢复后重新观测和控制。记录锁定比例、捕获失败率、首次锁定、失锁及重新锁定次数。`locked` 属于仿真评分，使用真值生成的数据测量，不作为控制器的输入。

默认快模式 40 轮/2 秒，正式模式 100 轮/5 秒；初始钟差 100 ns、SFO 0.2 ppm、CFO 600 Hz。速率游走为每根号秒 `1e-12`，本振频率游走为每根号秒 `0.02 Hz`。快/正式参考观测分别为 1/2 ms；原快模式 200 µs 的观测不足以稳定支撑 50 ms 保持期，因此延长观测而保留同一验收阈值。

稳态汇总排除前两轮捕获过程，但逐轮 CSV、锁定比例及失锁计数包含全部轮次。九点检查是离散保持期采样，并非连续时间上界，也不是无限期保持承诺。默认锁定标准为钟差和到达差都不超过 20 ps、相位不超过 0.1 rad、相对非相干功率和增益不低于 2.9 dB。

## 4. 联合 Monte Carlo

每次试验执行四时间戳交换、随机非零 CFO 的参考估计和控制、RX 到达反馈、相位反馈及真实 IQ 合成。CFO 默认在 ±1000 Hz 内均匀分布，观测 100 µs，分段 20 µs；这个独立试验的采样钟速率理想，非零采样钟频差的联合动态由上述持续实验覆盖。

时延、CFO 和相位使用独立 RNG 子流。新增频偏 RMSE 及捕获失败率；有效样本的 RMSE/增益属于条件统计，必须与失败率一起解释，全失败不输出成功结果。频偏配置须在分段相位展开的捕获范围内；超范围频率粗捕获仍需独立设计。

## 5. 运行与验收

在项目目录执行：

```powershell
& "C:\Users\ct183\anaconda3\envs\gpu_torch\python.exe" -m unittest discover -s tests -v
& "C:\Users\ct183\anaconda3\envs\gpu_torch\python.exe" main_demo.py --mode formal --output-dir results\joint_fixes_formal
& "C:\Users\ct183\anaconda3\envs\gpu_torch\python.exe" verify_results.py results\joint_fixes_formal
```

快速一键入口仍为 `./verify.ps1 -Mode fast_demo`。`summary.json` 保存持续统计，`joint_tracking.csv` 保存逐轮命令及保持期极值；`monte_carlo.csv` 保存每个 SNR 的时间、频率、失败率和增益。`run_config.json` 保存全部配置。验收 v2 同时检查瞬时和持续结果，不能用相位校正后的一个快照掩盖保持期失锁。

新测试覆盖 25 ns 差分路径校正、跨周期短码捕获、独立时钟改变 IQ、漏收/截断、随机 CFO 实际估计、捕获失败统计，以及第 5/6 轮掉帧叠加第 5 轮 100 Hz 频偏突变后的恢复。

### 本次验证记录

使用 Python 3.12.9、NumPy 1.26.4、SciPy 1.15.2、Matplotlib 3.10.1；全量 90 项测试通过，随后滑窗速率估计的 3 项持续跟踪回归也通过。最终快速、正式模式各 21 项输出验收全部通过。

| 指标 | 快速模式 | 正式模式 |
|---|---:|---:|
| 36 dB 双音时延 RMSE | 4.552 ps | 2.041 ps |
| 36 dB 双向钟差 RMSE | 3.133 ps | 1.455 ps |
| 持续实验轮数 / 时长 | 40 / 2 s | 100 / 5 s |
| 首次锁定轮次（从 0 计） | 1 | 1 |
| 锁定比例（包含首轮） | 97.5% | 99.0% |
| 首次锁定后失锁次数 | 0 | 0 |
| 稳态最大节点钟差 | 7.764 ps | 3.593 ps |
| 稳态最大目标到达差 | 19.485 ps | 10.261 ps |
| 稳态最大 IQ 相位误差 | 1.746° | 0.558° |
| 稳态最小相干增益 | 2.9973 dB | 2.9982 dB |

数值来自 `results/joint_fixes_fast/` 和 `results/joint_fixes_formal/`。保持期极值是在每周期 9 个时间点采样得出的极值；这与独立 Monte Carlo 的 RMSE、论文标准差均为不同统计量。故障恢复场景由单独集成测试验证，不与上述默认无掉帧运行混为同一实验。

## 6. 硬件边界

以上仍为浮点事件及带限 IQ 仿真。未实现 SDR 驱动、FPGA 定时分辨率、ADC/DAC 量化、真实相噪谱、RF 群时延温漂、多径和移动。独立时钟采样已作用于时间传递 burst；长下行数据流和独立 RX 自由振荡器尚未完整建模。上板后需将同样的观测/控制接口绑定定时收发、分数延时及 NCO，并做单独硬件标定。
